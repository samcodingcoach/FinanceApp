<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinanceApp.Pengaturan.SettingFinger"
             BackgroundColor="#f8fafc"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False"
             Shell.TabBarIsVisible="False">
             
    <Grid RowDefinitions="Auto,*">
        <!-- Custom Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Padding="20,20,20,15" BackgroundColor="White">
            <Grid.Shadow>
                <Shadow Brush="#000000" Opacity="0.05" Radius="5" Offset="0,2" />
            </Grid.Shadow>
            <Label Text="Pengaturan Biometrik" FontSize="18" TextColor="CornflowerBlue" FontAttributes="Bold" VerticalOptions="Center"></Label>
            <Label Text="KEMBALI" Grid.Column="1" FontSize="14" TextColor="CornflowerBlue" FontAttributes="Bold" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Back_Tapped" />
                </Label.GestureRecognizers>
            </Label>
        </Grid>

        <!-- Main Content -->
        <ScrollView Grid.Row="1" Padding="20">
            <VerticalStackLayout Spacing="25">
                
                <!-- Biometric Switch Card -->
                <Border BackgroundColor="White" Padding="20" StrokeShape="RoundRectangle 12" Stroke="#e2e8f0" StrokeThickness="1">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="8">
                        <Label Grid.Row="0" Grid.Column="0" Text="Login Cepat Biometrik" FontSize="16" FontAttributes="Bold" TextColor="#1e293b" />
                        <Label Grid.Row="1" Grid.Column="0" Text="Gunakan sidik jari atau Face ID untuk masuk ke aplikasi tanpa repot mengetik password." FontSize="13" TextColor="#64748b" LineBreakMode="WordWrap" Margin="0,0,10,0" />
                        <Switch x:Name="BiometricSwitch" Grid.RowSpan="2" Grid.Column="1" VerticalOptions="Center" OnColor="CornflowerBlue" Toggled="BiometricSwitch_Toggled" />
                    </Grid>
                </Border>

                <!-- Password Verification -->
                <VerticalStackLayout x:Name="PasswordContainer" Spacing="10" IsVisible="False">
                    <Label Text="Verifikasi Keamanan" FontSize="14" FontAttributes="Bold" TextColor="#475569" />
                    <Border Padding="15,0" StrokeShape="RoundRectangle 10" Stroke="#cbd5e1" StrokeThickness="1" BackgroundColor="White">
                        <Entry x:Name="E_Password" Placeholder="Masukkan password Anda" IsPassword="True" TextColor="#333" />
                    </Border>
                    <Label Text="Kami memerlukan password Anda untuk menyimpan pengaturan keamanan ini." FontSize="12" TextColor="#94a3b8" />
                </VerticalStackLayout>

                <!-- Save Button -->
                <Button x:Name="BtnSimpan" Text="SIMPAN PENGATURAN" CornerRadius="10" BackgroundColor="CornflowerBlue" TextColor="White" FontAttributes="Bold" HeightRequest="50" Margin="0,10,0,0" Clicked="BtnSimpan_Clicked" IsVisible="False" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>